<Window
    x:Class="WpfDesignAndAnimationLab.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:infrastructure="clr-namespace:WpfDesignAndAnimationLab.Infrastructure"
    xmlns:local="clr-namespace:WpfDesignAndAnimationLab"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:outlinedtext="clr-namespace:WpfDesignAndAnimationLab.AnimationDemos.OutlinedText"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ListBox
            x:Name="DemoItemsControl"
            MaxWidth="300"
            ItemsSource="{Binding Definitions}"
            SelectionChanged="ListBox_SelectionChanged"
            SelectionMode="Single">
            <ListBox.DataContext>
                <infrastructure:ExampleDefinitions />
            </ListBox.DataContext>
        </ListBox>
        <ContentControl x:Name="DemoContentControl" Grid.Column="1" />
    </Grid>
</Window>
